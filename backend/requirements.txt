# ── ALPR System — Python 3.11 + Windows x64 ──────────────────────────────────
# All packages have pre-built wheels — NO compiler required.

# NumPy — 2.x ships cp311-win_amd64.whl (1.26.4 did not → build error)
numpy>=2.0.0,<2.3.0

# OpenCV — cp37-abi3 wheel works on all Python 3.7+
opencv-python>=4.9.0,<4.11.0

# ONNX Runtime — used for BOTH YOLO detector AND CCT OCR (no PaddleOCR needed)
onnxruntime>=1.18.0,<2.0.0
# GPU variant (replace above if you have CUDA):
# onnxruntime-gpu>=1.18.0,<2.0.0

# MongoDB driver
pymongo>=4.6.1,<5.0.0

# Utilities
Pillow>=10.2.0,<12.0.0
requests>=2.31.0,<3.0.0
tqdm>=4.66.0,<5.0.0


